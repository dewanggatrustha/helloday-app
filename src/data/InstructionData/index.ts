import { earthquakeInformation, earthquakeInstruction } from "./earthquakeData";
import { fireInformation, fireInstruction } from "./fireData";
import { floodInformation, floodInstruction } from "./floodData";
import { landSlideInformation, landSlideInstruction } from "./landSlideData";
import { tornadoInformation, tornadoInstruction } from "./tornadoData";
import { tsunamiInformation, tsunamiInstruction } from "./tsunamiData";

export {
	earthquakeInformation,
	earthquakeInstruction,
	fireInformation,
	fireInstruction,
	floodInformation,
	floodInstruction,
	landSlideInformation,
	landSlideInstruction,
	tornadoInformation,
	tornadoInstruction,
	tsunamiInformation,
	tsunamiInstruction,
};
